apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ansible-mcp.fullname" . }}-test-connection"
  labels:
    {{- include "ansible-mcp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ansible-mcp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
